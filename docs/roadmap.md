# Roadmap (Next Backlog)

This roadmap is derived from `docs/backlog-status.md` and is intended for issue creation.

## Now (P1)
1. Sentence-level chunking for long single-line CT2 inputs (`area:engine`, `type:tech-debt`)
2. PPTX adapter MVP (`area:adapter`, `type:feature`)
3. XLSX adapter MVP (`area:adapter`, `type:feature`)
4. PDF-text adapter MVP (`area:adapter`, `type:feature`)

## Next (P2)
1. Structured CT2 generation config tuning (`area:engine`, `type:research`)
2. CI split: deterministic default + optional local CT2 checklist (`area:packaging`, `type:quality`)
3. CLI UX polish for progress/summary metadata (`area:cli`, `type:feature`)

## Backlog Hygiene
- Keep GitHub Issues as source of truth.
- Keep `docs/tasks` as execution contracts for Codex.
- Update `docs/backlog-status.md` after each merged task.
